%h1 Listing expenses

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @expenses.each do |expense|
    %tr
      %td= expense.name
      %td= link_to 'Show', expense
      %td= link_to 'Edit', edit_expense_path(expense)
      %td= link_to 'Destroy', expense, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Expense', new_expense_path
